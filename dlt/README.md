# dlt ingestion examples

This directory contains dlt-based ingestion blueprints and runner scripts.

## Contents

- Pipeline runners such as `run_dlt_pipelines.py`.
- Source-specific examples under folders like `google_sheets/` and `hubspot/`.
- Shared dependencies in `requirements.txt`.
