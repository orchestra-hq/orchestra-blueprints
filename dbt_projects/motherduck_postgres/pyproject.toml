[project]
name = "duckdb_example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "duckdb==1.5.2",
  "dbt-duckdb==1.10.1",
  "dbt-core==1.10.9"
]
