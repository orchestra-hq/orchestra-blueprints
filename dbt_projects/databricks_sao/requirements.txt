dbt-core==1.10.9
dbt-databricks==1.10.10
# State Aware Orchestration, pinned to the branch behind
# https://github.com/orchestra-hq/sao-paolo `feat/select-sources`, which scopes
# source freshness to the current selection (so `dbt build --select
# source:raw.raw_orders+` stops polling freshness for out-of-scope sources).
# This pin is the only thing this branch adds on top of main; drop it and the
# branch once that work is released.
dbt-orchestra @ git+https://github.com/orchestra-hq/sao-paolo.git@feat/select-sources
