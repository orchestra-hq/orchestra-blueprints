version: 2

models:
  - name: stg_orders
    description: "Typed view over the raw.raw_orders source."
    columns:
      - name: order_id
        description: "Primary key."
        tests:
          - unique
          - not_null
      - name: order_date
        tests:
          - not_null

  - name: stg_unused_events
    description: "Typed view over raw_unused.raw_events -- not part of the demo's --select, on purpose."
