seeds:
  - name: orders_raw
    description: Order data
    columns:
      - name: order_id
        description: Order identifier
        tests:
          - accepted_values:
              values: ["Moo"]
              config:
                severity: warn
                meta:
                  tags:
                    - "Data Tests"
                  owner: "gary@getorchestra.io"
                  channel: "#alert-testing"
                  subscribers:
                    - "gary@getorchestra.io"
