version: 2

models:
  - name: stg_orders
    description: "Typed view over the raw_orders seed."
    columns:
      - name: order_id
        description: "Primary key."
        tests:
          - unique
          - not_null
      - name: order_date
        tests:
          - not_null
