# Claude Agent

Orchestrates autonomous Claude agent execution for task automation using the Claude Agent SDK. The main component is `agent.py`, which manages agent creation and execution with configurable tools (Read, Edit, Glob, Bash) for autonomous workflows. Integrates with GitHub for seamless task automation and Orchestra SDK for workflow orchestration.
